package main

import "fmt"
/*
func main() {
	//var a [5]int //declaring arrays
	var myArray = [5]int{1, 2, 3, 4, 5} // declaring and intializing arrays

	fmt.Println(len(myArray)) // len of the array

	for i := 0; i < len(myArray); i++ {
		fmt.Println(myArray[i], " Is at element ", i)
	}
	*/
}
